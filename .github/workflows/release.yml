name: release
run-name: Release by @${{ github.actor }} from ${{ github.ref_name }}

on: [push]

jobs:
  release:
    if: ${{ github.ref_type == 'branch' }}
    uses: nextmv-io/release/.github/workflows/release.yml@feature/adjust-go-version-location
    with:
      BRANCH: ${{ github.ref_name }}
      REPOSITORY: go-mip
      LANGUAGE: go
      PACKAGE_NAME: go-mip
      PACKAGE_LOCATION: ./
      VERSION_FILE: VERSION
    secrets: inherit

  notify:
    needs: [release]
    if: ${{ needs.release.outputs.RELEASE_NEEDED == 'true' && needs.release.outputs.SHOULD_NOTIFY_SLACK == 'true' }}
    uses: nextmv-io/release/.github/workflows/notify-slack.yml@develop
    with:
      PACKAGE_NAME: go-mip
      VERSION: ${{ needs.release.outputs.VERSION }}
      REPOSITORY: go-mip
    secrets: inherit
